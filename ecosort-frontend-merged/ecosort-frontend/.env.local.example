# Base URL of the EcoSort Spring Boot API (no trailing slash).
# Exposed to the browser (NEXT_PUBLIC_ prefix) because the SPA calls
# this API directly from client components — see lib/api/axiosClient.ts.
NEXT_PUBLIC_API_BASE_URL=https://ecosort-backend-d8hp.onrender.com
